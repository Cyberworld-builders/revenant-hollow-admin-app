import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Revenant Hollow Admin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
